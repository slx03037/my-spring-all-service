server:
  port: 8095

spring:
  application:
    name: admin-server-security
  boot:
    admin:
      server:
        enabled: true
  security:
    user:
      password: '123456'
      name: 'admin'
management:
  endpoints:
    web:
      exposure:
        include: "*"
#      base-path: /actuator
  endpoint:
    health:
      show-details: always


